<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NOTY.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NOTY"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NOTY"
    >

    <Grid Background="#202020">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top header row, fixed 30px, spans both columns -->
        <Border Grid.Row="0" Background="#181818">
            <TextBlock Text="NOTY" FontSize="14" FontWeight="Bold" Foreground="#ffffff" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- Middle section: Files list + buttons (left) + TEXT/switch + TextBox + hotkeys (right) -->
        <Grid Grid.Row="1" Background="#202020">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*"/>
                <ColumnDefinition Width="70*"/>
            </Grid.ColumnDefinitions>

            <!-- Faux Mica background layers (XAML fallback when system Mica/Acrylic unavailable) -->
            <Border Grid.ColumnSpan="2" Background="{StaticResource CustomAcrylicInAppLuminosity}" Opacity="1"/>

            <!-- subtle overlay to mimic Mica depth -->
            <Rectangle Grid.ColumnSpan="2" IsHitTestVisible="False">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#00FFFFFF" Offset="0"/>
                        <GradientStop Color="#0F000000" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
                <Rectangle.Opacity>0.06</Rectangle.Opacity>
            </Rectangle>

            <!-- Left column: Files list + buttons -->
            <Grid Background="#202020">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="60*"/>
                    <RowDefinition Height="40*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" CornerRadius="12" Background="#202020">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Padding="20,16,20,12" Background="Transparent">
                            <TextBlock Text="Files" FontSize="20" FontWeight="SemiBold" Foreground="#ffffff"/>
                        </Grid>

                        <ListView x:Name="FilestoSave" SelectionChanged="FilestoSave_SelectionChanged" Grid.Row="1" Margin="12,8,12,12" SelectionMode="Single" Background="Transparent">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>

                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Padding" Value="12,8" />
                                    <Setter Property="Margin" Value="0,6" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="16" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="#111111">
                    <StackPanel Padding="12" Spacing="8">
                        <Button Click="RefreshButton" Content="Refresh" HorizontalAlignment="Stretch"/>
                        <Button Click="TweaksButton" Content="Tweaks" HorizontalAlignment="Stretch"/>

                        <Button Click="ChangePathButton" Content="Change Path To Save" HorizontalAlignment="Stretch"/>

                        <HyperlinkButton x:Name="GithubButton" Content="My Github" NavigateUri="https://github.com/Arqam-Gh" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Right column: TEXT + toggle switch row, then TextBox, then hotkeys -->
            <Grid Grid.Column="1" Background="#202020">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- TEXT + toggle switch, right below header, above TextBox -->
                <Grid Grid.Row="0" Background="#111114" Padding="10,-2,10,-10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="TEXT" Foreground="#ffffff" VerticalAlignment="Center"/>

                    <ToggleSwitch x:Name="ThemeToggle" Grid.Column="1" OnContent="Dark" OffContent="Light" 
                                  VerticalAlignment="Center"/>
                </Grid>

                <Border Grid.Row="1" Background="#111114">
                    <TextBox Margin="5,10,5,10" x:Name="TextInFileToDisplay" AcceptsReturn="True" TextWrapping="Wrap"/>
                </Border>

                <Border Grid.Row="2" Background="#111114" Padding="12,8,12,8">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Foreground="#cccccc" FontSize="13">
                            <Run Text="HOTKEYS:" FontWeight="SemiBold"/>
                            <LineBreak/>
                            <Run Text="Save Selected Text: Ctrl+Alt+S"/>
                            <LineBreak/>
                            <Run Text="Make New File: Ctrl+Alt+N"/>
                            <LineBreak/>
                            <Run Text="Kill Noty: Ctrl+Alt+X"/>
                        </TextBlock>

                        <Button Click="Save_to_file" Content="Save" Grid.Column="1" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Window>
